---
description: 
globs: *.tsx,*.json,*.ts,*.js.,*.yml
alwaysApply: false
---

**@TODO правило обработки задач в файле TODO:**

1. **Структура:**

   * `+` — задача **выполнена**.
   * `-` — задача **не выполнена**.
   * Порядок задач важен — **обработка всегда начинается с первой невыполненной задачи**.

2. **Контекст выполнения:**

   * Все мои действия, ответы и команды должны быть **строго в контексте первой задачи с пометкой `-`**.
   * **Нельзя** переходить ко второй или следующим задачам, пока первая `-` задача не будет явно отмечена как выполненная (`+`).

3. **Ограничения:**

   * **Не выходить за рамки текущей задачи.**
   * Не интерпретировать задачи из других источников, кроме текущего `TODO` файла.
   * Ответы должны быть сфокусированы только на том, что относится к **активной задаче**.

4. **После выполнения:**

   * Задача должна быть вручную или автоматически отмечена как `+` (выполнена).

   * Только после этого переходим к следующей `-` задаче в списке.